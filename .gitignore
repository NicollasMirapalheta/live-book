node_modules
dist
shots

# Variáveis de ambiente locais — nunca commitar segredos. O template versionado
# é o .env.example.
.env
.env.*
!.env.example

# Playwright (MEDIA-11)
test-results
playwright-report
